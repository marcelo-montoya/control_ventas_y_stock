<%= form_with(model: costumer) do |form| %>
  <% if costumer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(costumer.errors.count, "error") %> prohibited this costumer from being saved:</h2>

      <ul>
        <% costumer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "form-label" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :email, style: "form-label" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.submit "Actualizar cliente", class:"btn btn-outline-secondary" %>
  </div>
<% end %>
